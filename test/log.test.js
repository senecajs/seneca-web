'use strict'

var Lab = require('lab')

var lab = exports.lab = Lab.script()
var describe = lab.describe
var it = lab.it

describe('log adapter', () => {
  it('logs mapped routes to STDOUT', (done) => {
    done()
  })
})
